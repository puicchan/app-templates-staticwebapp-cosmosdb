# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: app-templates-staticwebapp-cosmosdb
services:
  web:
    project: ./src/web
    dist: dist
    language: js
    host: staticwebapp
  api:
    project: ./src/api
    language: js
    host: function